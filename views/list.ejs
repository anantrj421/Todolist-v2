<%- include('header'); -%>
    <body>
        <div class="box" id="heading">
            <h1><%=day%></h1>
        </div>
        <div class="box">
            <%newi.forEach(function(it){%>
            <form action="/delete" method="POST"><div class="item">
                <input type="checkbox" onChange="this.form.submit()" name="cb" value=<%=it._id%>>
                <p><%=it.name%></p>
            </div>
            <input type="hidden" name="lname" value="<%=day%>">
        </form>
        <%});%>
            <form class="item" action="/" method="POST">
                <input type="text" name="t1" placeholder="New Item" autocomplete="off">
                <button type="submit" name="btn" value="<%=day%>">+</button>
            </form>
        </div> 
    </body>
<%- include('footer'); -%>  